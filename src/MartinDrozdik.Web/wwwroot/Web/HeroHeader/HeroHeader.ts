import { WindowEvents } from "@drozdik.m/window-events";


WindowEvents.OnDOMReady.Add(function ()
{
    var opacities = [0.0175, 0.05, 0.1, 0.25]
    var contentElement = document.getElementById("heroHeaderContent");
});