using Microsoft.AspNetCore.Html;
using Microsoft.AspNetCore.Mvc.ViewFeatures;

namespace MartinDrozdik.Web.ViewSupport.HtmlExtensions
{
    public static class HtmlHelperExtensions
    {
        /*public static IHtmlContent InlineStyles(this HtmlHelper htmlHelper, string resourcePath)
        {

            string bundleContent = LoadResourceContent(htmlHelper.ViewContext.HttpContext, bundleVirtualPath);
            string htmlTag = string.Format("<style>{0}</style>", bundleContent);

            return new HtmlString(htmlTag);
        }*/
    }
}
